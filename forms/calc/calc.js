function Calculate (numberone, numbertwo) {
  return numberone + numbertwo
  }
  
let num1 = Number(prompt("Enter in a number"))
let num2 = Number(prompt("Enter in a second number"))

let summedNumbers = Calculate(Number(num1), Number(num2))

console.log(summedNumbers)
